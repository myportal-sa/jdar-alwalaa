# خطوات ربط الموقع بجوجل شيت

## 1) جهّز الشيت
1. افتح Google Sheets وأنشئ ملف جديد.
2. في الصف الأول اكتب هذه العناوين بالضبط: `الاسم` | `الرسالة` | `التاريخ`

## 2) أضف كود الربط (Apps Script)
1. من القائمة: **Extensions → Apps Script**.
2. احذف الكود الموجود، والصق محتوى ملف `Code.gs` المرفق كاملًا.
3. احفظ المشروع (أيقونة الحفظ أو Ctrl+S).

## 3) انشر المشروع كتطبيق ويب
1. أعلى يمين محرر السكربت اضغط **Deploy → New deployment**.
2. اختر النوع: **Web app**.
3. اضبط:
   - **Execute as:** Me (حسابك)
   - **Who has access:** Anyone
4. اضغط **Deploy**، ووافق على أذونات الوصول لحسابك.
5. انسخ الرابط الذي يظهر لك (ينتهي بـ `/exec`).

## 4) الصق الرابط في الموقع
افتح كل من الملفين التاليين وابحث عن هذا السطر، ثم استبدل النص بالرابط الذي نسخته:

```js
const CONFIG = {
  SHEET_API_URL: "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE"
};
```

- `index.html` (صفحة إضافة التهنئة)
- `wall.html` (صفحة جدار الولاء)

## 5) جرّب الموقع
- افتح `index.html`، اكتب اسمًا ورسالة، واضغط **إضافة إلى جدار الولاء**.
- ستنتقل تلقائيًا إلى `wall.html` وسترى تهنئتك ضمن البطاقات.
- كل تهنئة جديدة تُضاف كصف جديد في الشيت مباشرة.

## ملاحظات
- كل مرة تعدّل فيها كود Apps Script، تحتاج عمل **New deployment** جديد (أو Manage deployments → Edit → New version) حتى تنعكس التعديلات.
- لعرض الموقع على الإنترنت (وليس فقط على جهازك)، ارفع مجلد الموقع كاملًا (index.html, wall.html, مجلد assets, وليس ملفات الإعداد) إلى استضافة مجانية مثل GitHub Pages أو Netlify.
